import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'apple-payment',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
